import { BorderChangeOnHoverDirective } from './border-change-on-hover.directive';

describe('BorderChangeOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderChangeOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
